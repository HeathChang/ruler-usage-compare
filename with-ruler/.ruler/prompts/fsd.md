# Feature-Sliced Design (FSD) Rules

## 기본 원칙 (강제)
- 모든 코드는 반드시 FSD 레이어 중 하나에 속해야 한다.
- 레이어를 건너뛰는 의존성은 금지한다.
- "일단 여기" 식의 파일 배치는 허용하지 않는다.

---

## 레이어 정의

### app
- 애플리케이션 초기화 코드
- 전역 Provider (Theme, QueryClient 등)
- 전역 에러 처리, 라우팅 설정

허용 예:
- `App.tsx`
- `providers/`
- `store/`

금지:
- 비즈니스 로직
- UI 컴포넌트

---

### pages
- 라우트 단위의 페이지
- URL / path param / query 검증
- 페이지 조립 책임만 가진다.

규칙:
- 직접적인 비즈니스 로직 금지
- feature / widget을 조합만 한다.

---

### widgets
- 페이지를 구성하는 **큰 UI 블록**
- 여러 feature / entity를 조합할 수 있다.

예:
- `Header`
- `Sidebar`
- `CommentListWithInput`

---

### features
- 사용자 행동 단위
- 하나의 “동사”를 표현한다.

예:
- `login`
- `signup`
- `submitComment`

포함 가능:
- UI
- hooks
- API 호출
- 상태 관리

---

### entities
- 도메인 모델 중심
- 데이터 구조와 그에 대한 최소한의 로직

예:
- `user`
- `post`

포함 가능:
- type / interface
- entity 단위 API
- entity 전용 UI (Avatar, NameTag 등)

---

### shared
- 특정 도메인에 종속되지 않는 코드
- 가장 재사용성이 높은 영역

예:
- 공통 UI 컴포넌트
- 공통 hooks
- utils
- constants
- 디자인 토큰

---

## 의존성 규칙 (🔥 중요)

허용 방향 (아래 방향만 가능):
app
↓
pages
↓
widgets
↓
features
↓
entities
↓
shared


금지 예:
- `entities` → `features` import ❌
- `shared` → `entities` import ❌
- `features` → `pages` import ❌

---

## 파일 위치 결정 규칙 (AI 행동 규칙)

코드를 작성하기 전에 반드시 다음을 판단한다:

1. 이 코드는 **라우트 책임인가?** → `pages`
2. 사용자 행동 단위인가? → `features`
3. 도메인 모델인가? → `entities`
4. 재사용 UI / 유틸인가? → `shared`
5. 여러 개를 조합한 큰 블록인가? → `widgets`

판단이 불가능한 경우:
- **작성을 멈추고 질문한다**

---

## 네이밍 규칙
- feature / entity 폴더명은 **소문자**
- 동사/명사 기반으로 명확히 작성한다.

예:
- `features/login`
- `entities/user`

